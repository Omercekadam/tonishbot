# Çekirdek
discord.py>=2.4,<3
python-dotenv>=1.0

# HTTP — steam.py doğrudan kullanıyor.
# (discord.py zaten bağımlı olduğu için önceden çalışıyordu ama beyan edilmemişti.)
aiohttp>=3.9

# Veritabanı
aiosqlite>=0.20

# Görsel üretimi (liderlik tablosu, hoş geldin görseli, gamer kart)
Pillow>=10.0

# Zaman dilimi (!etkinliksayaci — Europe/Istanbul)
pytz>=2024.1

# Yapay zeka.
# NOT: google-generativeai artık bakımı bırakılan eski SDK (resmi destek
# 30 Kasım 2025'te sona erdi), yerine google-genai geçti. Göç ayrı bir iş
# olduğu için şimdilik son legacy sürümde kalıyoruz — 0.7.1, Gemini 3.x'in
# thinking_config parametresinden önceki bir sürümdü, bu yüzden 0.8.6'ya
# yükseltildi (bkz. cogs/ai.py'deki thinking_budget ayarı).
google-generativeai==0.8.6
